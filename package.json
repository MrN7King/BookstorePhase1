{
  "name": "bookstorephase1",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "frontend": "npm --prefix frontend run dev",
    "backend": "npm --prefix backend run dev"
  },
  "dependencies": {
    "axios": "^1.10.0"
  }
}
